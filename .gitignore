# Playwright debug artifacts (only at repo root, not in assets/)
/*.png
.playwright-mcp/

# Vercel CLI link (created when running vercel CLI from the worktree)
.vercel/
.vercel
.env*.local
.claude/settings.local.json
